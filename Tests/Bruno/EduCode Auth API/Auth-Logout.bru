meta {
  name: Auth/Logout
  type: http
  seq: 3
}

post {
  url: http://localhost:5139/api/Auth/Logout
  body: json
  auth: inherit
}

body:json {
  {
    "jwtToken": "JWT",
    "refreshToken": "REFRESHTOKEN",
    "client": "Bruno"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
